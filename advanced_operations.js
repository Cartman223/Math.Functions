function exponentiation(base) {
    console.log(`${base} squared is...${(base * base).toLocaleString("pt-BR")}!`);
    console.log(`${base} cubed is...${(base * base * base).toLocaleString("pt-BR")}!`);
  }
  
  
 /* exponentiation(1);
  exponentiation(2);
  exponentiation(3);
  exponentiation(4); */




function squareRoot(radicand) {
    console.log(`The square root of ${radicand} is ${Math.sqrt(radicand)}!`);
}


squareRoot(1);
squareRoot(2);
squareRoot(3);
squareRoot(4);
squareRoot(5);